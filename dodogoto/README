Desc:
	Add a simple tag (e.g. a) to a frequently used folder, then jump to it using simple command:
		g a

for Cshell users:
	alias g "source ~/bin/dodo_goto.csh"

for Bash users:
	alias g="source ~/bin/dodo_goto.bash"

Usage:
Add tag:
	g add TAGNAME
Jump to a tag:
	g TAGNAME
List available tags:
	g   OR   g ls
Delete a tag:
	g del TAGNAME
Delete all tags:
	g del all  ( Do not use 'all' as tag name )

Tag names not recommended:
	all ls del add

